
export enum FacilityType {
    Truck = 'Truck',
    PushCart = 'Push Cart',
  }
  
export enum Status {
    APPROVED = 'APPROVED',
    REQUESTED = 'REQUESTED',
    EXPIRED = 'EXPIRED',
    SUSPEND = 'SUSPEND',
    ISSUED = 'ISSUED',
  }